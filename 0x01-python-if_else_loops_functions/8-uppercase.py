#!/usr/bin/python3
def uppercase(s):
    """Print s in uppercase followed by a new line"""
    result = 
    for c in s:
        if ord(a) <= ord(c) <= ord(z):
            c = chr(ord(c) - 32)
        result += c
    print(result)

